{
  "name": "transient-flag-only",
  "branch": "FLAG_TRANSIENT (0x0008) set alone — isTransient() returns truthy (8), the minimal case exercising the true branch at both Ticket::transfer() and Ticket::update()",
  "input": {
    "flags": 8
  },
  "expected": 8
}
