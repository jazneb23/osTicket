{
  "name": "transient-plus-active",
  "branch": "flags=9 (FLAG_ACTIVE|FLAG_TRANSIENT): combined mask including target bit alongside another, bitwise-AND isolation still returns truthy (8) unaffected by the extra FLAG_ACTIVE bit",
  "input": {
    "flags": 9
  },
  "expected": 8
}
