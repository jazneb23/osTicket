{
  "name": "flags-escalate-bit-only",
  "branch": "FLAG_ESCALATE (2) alone set → non-zero flags && truthy FLAG_ESCALATE literal → true; matches both buggy and intended semantics, so does not by itself prove the bug",
  "input": {
    "flags": 2
  },
  "expected": "true"
}
