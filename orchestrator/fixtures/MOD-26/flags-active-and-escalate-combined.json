{
  "name": "flags-active-and-escalate-combined",
  "branch": "FLAG_ACTIVE | FLAG_ESCALATE (3) both set: `3 && 2` is true — combined-bits normal path where escalation bit is genuinely present alongside another bit",
  "input": {
    "flags": 3
  },
  "expected": "true"
}
