{
  "name": "transient-bit-alone-no-escalate",
  "branch": "Bug-exposed path: only FLAG_TRANSIENT (0x0008) set, FLAG_ESCALATE absent -> && expression still truthy due to the pre-existing &&-vs-& bug",
  "input": {
    "flags": 8
  },
  "expected": true
}
