{
  "name": "custom-numbers-only-not-active",
  "branch": "Only unrelated bit FLAG_CUSTOM_NUMBERS (0x0001) set, FLAG_ACTIVE absent -> returns false, confirming isolation from unrelated bits",
  "input": {
    "flags": 1
  },
  "expected": false
}
