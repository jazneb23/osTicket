{
  "name": "active-plus-custom-numbers",
  "branch": "Active combined with FLAG_CUSTOM_NUMBERS (0x0001): FLAG_ACTIVE|FLAG_CUSTOM_NUMBERS = 3 -> must still yield true since FLAG_ACTIVE bit is present",
  "input": {
    "flags": 3
  },
  "expected": true
}
