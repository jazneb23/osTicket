{
  "name": "active-plus-custom-numbers",
  "branch": "flags=3 (FLAG_CUSTOM_NUMBERS | FLAG_ACTIVE): active bit set alongside an unrelated flag — proves other set bits don't suppress the active result, isActive() returns true",
  "input": {
    "flags": 3
  },
  "expected": "true"
}
