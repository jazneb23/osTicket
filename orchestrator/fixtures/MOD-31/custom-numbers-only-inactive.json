{
  "name": "custom-numbers-only-inactive",
  "branch": "flags=1 (FLAG_CUSTOM_NUMBERS alone, FLAG_ACTIVE unset): unrelated bit set but active bit absent — isActive() returns false",
  "input": {
    "flags": 3
  },
  "expected": "true"
}
