{
  "name": "active-only",
  "branch": "FLAG_ACTIVE bit set alone (flags=2): isActive() returns true via `!!(2 & 2)`; isEnabled() delegates to the same true",
  "input": {
    "flags": 2
  },
  "expected": "{\"isActive\":true,\"isEnabled\":true}"
}
