.htaccess
php53.cgi
include/ost-config.php
setup/cli/modules/importer
*.sw[a-z]
.DS_Store
.vagrant
._.DS_Store
Vagrantfile
# VS Code workspace files
*.code-workspace

# Staging directory used for packaging script
stage

# Ignore packaged plugins and language packs
*.phar

# Ignore mPDF temp files
include/mpdf/ttfontdata
include/mpdf/tmp
nbproject/
.env
node_modules/

# Pipeline runtime cache (seam manifests per ticket)
orchestrator/.state/

# Verifier-generated parity reports (regenerated on every run, never a golden fixture)
orchestrator/fixtures/*/parity.json

# Local-only staff login seed for clicking around :8080 during a demo
legacy/seed/enable-staff-login.php
