#!/usr/bin/env bash
# Nested withCloudAgent VMs (cartographer, fixture-generator, pr-agent).
# Parity Docker runs on your Mac — not in this cloud VM.
echo "Cloud agent ready. Parity stack: bash scripts/local-demo-start.sh on your Mac."
