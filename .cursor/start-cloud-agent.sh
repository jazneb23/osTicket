#!/usr/bin/env bash
# Nested withCloudAgent VMs (cartographer, fixture-generator).
# Parity Docker / compose / listener run on the operator's Mac — never here.
# Do not start dockerd, docker compose, or scripts/local-demo-start.sh in this VM.
echo "Cloud agent ready (no Docker). Read code and finish your stage JSON only."
echo "Parity stack stays on the Mac: bash scripts/local-demo-start.sh"
